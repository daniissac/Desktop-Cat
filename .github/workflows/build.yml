name: Build KIND

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]
      fail-fast: false

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install PyQt6 pyinstaller
      - name: Build with PyInstaller
        run: pyinstaller --distpath dist/CatPet --onefile main.py
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: CatPet-${{ matrix.os }}
          path: dist/CatPet

  release:
    needs: build
    runs-on: [windows-latest, macos-latest]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4
      - name: Create Release
        id: create_release
        uses: actions/create-release@latest
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          tag_name: v${{ github.run_number }}
          release_name: Release ${{ github.run_number }}
          draft: false
          prerelease: false
      - name: Download artifacts
        uses: actions/download-artifact@v4
      - name: Zip artifacts
        run: |
          os_name=$(echo ${{ runner.os }} | tr '[:lower:]' '[:upper:]')
          if [ -d "CatPet-${{ matrix.os }}" ]; then
            zip -r "CatPet-$os_name.zip" "CatPet-${{ matrix.os }}"
          else
            echo "Directory CatPet-${{ matrix.os }} not found, skipping..."
          fi
      - name: Upload Release Assets (using latest version, with error handling)
        uses: actions/upload-release-asset@latest
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./dist/CatPet-Windows.zip
          asset_name: CatPet-Windows.zip
          asset_content_type: application/zip
      - name: Upload Release Assets (MacOS) (using latest version, with error handling)
        uses: actions/upload-release-asset@latest
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./dist/CatPet-MacOS.zip
          asset_name: CatPet-MacOS.zip
          asset_content_type: application/zip
